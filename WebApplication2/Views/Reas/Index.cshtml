@model IEnumerable<WebApplication2.Models.Rea>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.rea)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.numberBed)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.hospital)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.rea)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.numberBed)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.hospital.nameHospital)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
